import { COLORS } from "../../colors/colors";
export const styles = {
  container: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
  },
  icon: {
    color: COLORS.accentColor,
    fontSize: 35,
    padding: 10,
  },
};
